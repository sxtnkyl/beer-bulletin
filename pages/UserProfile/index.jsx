//wrap in auth state hook to dynamically render auth component
//example: authStat !== "user" ? <SignUp or SignIn /> : <Logout />
const UserProfile = () => {
  return <>im the user's profile</>;
};

export default UserProfile;
